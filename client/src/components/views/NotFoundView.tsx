import React from 'react';

// style stuff
import { Container } from '../styled/lib';

export const NotFoundView = () => {
    return (
        <Container>
            <h1>That's a 404...</h1>
        </Container>
    );
};
