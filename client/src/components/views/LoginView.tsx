import React from 'react';

// style stuff
import { Container } from '../styled/lib';

export const LoginView = () => {
    return (
        <Container>
            <h1>Login</h1>
        </Container>
    );
};
